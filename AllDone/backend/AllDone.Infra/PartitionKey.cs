using System.Dynamic;

namespace AllDone.Infra;

public static class PartitionKey
{
    public static int Get(object request)
    {
        switch(request)
        {
            case Rez addUser: return addUser.
        }
    }

}
